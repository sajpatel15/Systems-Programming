#include <stdio.h>

void fill(int* b, int* cnt){
  *count = 6;
  for (int i = 0; i < *cnt; i++){
    b[i] = i*2;
  }

}

int main(){
  int a[10];

  int count;

  fil(a, &count);

  for(int i = 0; i < count; i+){
    printf("in main: %d: %d\n",i, a[i]);
  }
}
